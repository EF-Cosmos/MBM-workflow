## 清理分支：合并 Blender 5.2 适配 + 删除残留分支

### 1. 合并功能分支（fast-forward，无冲突）
- 在当前 `main` 上执行 `git merge feat/blender52-adaptation`
- main 将快进到 cad8760f「适配 Blender 5.2 LTS（Python 3.13）」
- 该提交只改源码/wheels/文档，与工作区未提交改动（.claude/settings.local.json 等）无交集

### 2. 推送合并结果
- `git push origin main`

### 3. 删除本地分支
- `git branch -d feat/blender52-adaptation`（已合并，安全删除）
- `git branch -d main.sync-conflict-20260213-231745-6XCXGC7`（完全包含于 main，安全删除）

### 4. 删除远端分支
- `git push origin --delete feat/blender52-adaptation`
- `git push origin --delete main.sync-conflict-20260213-231756-6XCXGC7`
  （注意：远端冲突分支的时间戳是 **231756**，与本地 **231745** 不同，是两个不同名字的分支，都要删）

### 5. 验证
- `git branch -a` 确认最终只剩 `main`（本地+远端）

完成后仓库将只有 main 一个分支，指向 Blender 5.2 适配后的最新代码。